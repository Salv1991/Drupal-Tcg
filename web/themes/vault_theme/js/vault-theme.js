/**
 * @file
 * vault_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.vaultTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
